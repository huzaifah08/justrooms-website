# Use a lightweight Node.js image
FROM node:16-alpine

# Set working directory
WORKDIR /app

# Copy package files first
COPY package*.json ./

# Install dependencies
RUN npm install

# Copy the application source code
COPY . .

# Expose the port your Fastify server listens on (for example, 3001)
EXPOSE 8080

# Start the Fastify server – adjust the entrypoint as needed (e.g., if your entry file is server.js)
CMD ["node", "server.js"]
